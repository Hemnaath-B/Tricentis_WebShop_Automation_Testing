package com.searchandfiltersteps;\n\nimport org.openqa.selenium.*;\nimport io.cucumber.java.en.*;\nimport org.junit.Assert;\n\npublic class SearchForBooksSteps {\n\n    WebDriver driver;\n\n    @Given(\"the user is on the homepage\")\n    public void the_user_is_on_the_homepage() {\n        // TODO: implement\n        driver.get(\"http://example.com\");\n    }\n\n    @When(\"the user enters {string} in the search bar\")\n    public void the_user_enters_in_the_search_bar(String bookTitle) {\n        // TODO: implement\n        WebElement searchBar = driver.findElement(By.id(\"search-bar\"));\n        searchBar.sendKeys(bookTitle);\n    }\n\n    @And(\"clicks the search button\")\n    public void clicks_the_search_button() {\n        // TODO: implement\n        WebElement searchButton = driver.findElement(By.id(\"search-button\"));\n        searchButton.click();\n    }\n\n    @Then(\"the user should see a list of books related to {string}\")\n    public void the_user_should_see_a_list_of_books_related_to(String bookTitle) {\n        // TODO: implement\n        WebElement results = driver.findElement(By.id(\"search-results\"));\n        Assert.assertTrue(results.getText().contains(bookTitle));\n    }\n\n    @And(\"each book should display its title, author, and price\")\n    public void each_book_should_display_its_title_author_and_price() {\n        // TODO: implement\n        WebElement results = driver.findElement(By.id(\"search-results\"));\n        Assert.assertTrue(results.getText().contains(\"Title\"));\n        Assert.assertTrue(results.getText().contains(\"Author\"));\n        Assert.assertTrue(results.getText().contains(\"Price\"));\n    }\n}\n